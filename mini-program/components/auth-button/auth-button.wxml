<view class="auth-button-container">
  <view class="auth-content">
    <text class="auth-title">{{title}}</text>
    <text class="auth-desc">{{desc}}</text>
    <button class="btn btn-primary auth-btn" bindtap="onAuthorize">{{buttonText}}</button>
  </view>
</view>